# --- Planning / spec / AI artifacts ---
# Fork change: upstream never commits these; we DO. CLAUDE.md and the .claude/ skills are
# the fork's working documentation and travel with the repo. Only the machine-local
# settings file stays out.
.claude/settings.local.json

# --- Gradle ---
.gradle/
build/
**/build/

# --- Android Studio / IntelliJ ---
.idea/
*.iml
*.iws
*.ipr
captures/
.cxx/
output.json

# --- Local machine config (SDK path, secrets) ---
local.properties
*.jks
*.keystore
keystore.properties

# --- Misc ---
.DS_Store
*.log
